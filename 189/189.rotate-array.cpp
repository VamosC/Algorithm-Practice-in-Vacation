/*
 * @lc app=leetcode id=189 lang=cpp
 *
 * [189] Rotate Array
 */
class Solution
{
public:
    void rotate(vector<int> &nums, int k)
    {
        uint64_t size = nums.size();
        k = k % size;
        for (auto i = 0; i < k; i++)
        {
            for (auto)
        }
    }
};
